<p id="notice"></p>

<p>
<div class="title">
	<strong>Title:</strong>
</div>
  <%= @post.title %>
  <div class="palindrome" >Insert Palindrome
  </div>
  <div class="detect_palindrome" >Print Palindrome Results
  </div>
  <div class="detect_smallest_palindrome">Detect Smallest Palindrome Here
  </div>

</p>

<p>
  <strong>Photo:</strong>
  <div class="image">
    <% if @post.photo.attached? %>
      <%= image_tag(url_for @post.photo) %>
    <% end %>
  </div>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
